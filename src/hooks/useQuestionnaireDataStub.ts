// Stub functions for questionnaire data management
// These functions are temporarily disabled

export function getParticipantById(id: string) {
  console.warn('getParticipantById is not currently available');
  return Promise.resolve(null);
}

export function updateParticipantQuestionnaireById(id: string, data: any) {
  console.warn('updateParticipantQuestionnaireById is not currently available');
  return Promise.resolve(null);
}

export function updateParticipantInterview(id: string, data: any) {
  console.warn('updateParticipantInterview is not currently available');
  return Promise.resolve(null);
}

export function updateParticipantConsentById(id: string, data: any) {
  console.warn('updateParticipantConsentById is not currently available');
  return Promise.resolve(null);
}

export function createParticipant(data: any) {
  console.warn('createParticipant is not currently available');
  return Promise.resolve(null);
}

export function getParticipantByEmail(email: string) {
  console.warn('getParticipantByEmail is not currently available');
  return Promise.resolve(null);
}